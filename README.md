# buckleberry

Buckleberry is a simple service that exposes content from several common self-hosting services via an Open Publishing Distribution System (OPDS) interface. I built this service mostly for myself, and I use it to get articles onto my Xteink X4, but it should work with any standard OPDS client.

### Supported Services

- [X] Wallabag
- [X] Linkding
- [ ] ??

## AI Usage Disclosure

Nearly all of this repository now contains only human-generated code. Some of it - a few tests and some of the infrastructure around the database - were originally generated by an LLM, and you'll see Claude's name if you go back through the history. I intend to replace the rest of the AI code over time, but I'll leave this disclosure here.

## Running

I run Buckleberry in a Coolify instance, and so it comes with a Dockerfile to facilitate that. The Dockerfile is a "distro-less" image; it's completely self-contained and very small. If you need to run the app manually, there's a Make directive:

```bash
make run
```

When debugging the app, there's also a handy `watch` directive that spins up a proxy server to reboot the app whenever you make a file change:

```bash
make watch
```

The first time you run the app, you'll need to onboard by setting up connections to one or more of the supported services. You'll also set up a username and password; these are used both to log in and to authenticate the OPDS server.

## Configuration

The app has a few optional env vars and one required one:

| Variable        | Default                  | Description                                        |
| --------------- | ------------------------ | -------------------------------------------------- |
| **`BASE_URL`**      | `http://localhost:$PORT` | **Required**. The base public URL for OPDS feed links.         |
| `DB_PATH`       | `./buckleberry.db`       | SQLite database path.                              |
| `PORT`          | `8080`                   | Port to listen on.                                 |

## Deployment

The `Dockerfile` builds a static, CGO-free binary onto `distroless/static`.
 
For [Coolify](https://coolify.io/):

1. Point a new resource at this repo; it builds the `Dockerfile` automatically.
2. Add a **persistent volume mounted at `/data`** — the SQLite DB lives there and would otherwise reset on redeploy.
3. Set env vars:
   - `BASE_URL` → your public URL (e.g. `https://buckleberry.example.com`). Required — OPDS links are built from it.

## Development

Buckleberry runs on a very simple and self-contained tech stack. The core app is built using Golang and the Fiber web framework. All the views are built with Templ, which is so good I started writing all of my web apps in Go. Front-end interactivity is very limited, but what's there is built with Alpine.js and HTMX.

Templ templates must be compiled into Go. The makefile has two commands to do this - `make generate` for one-time generation and `make watch` to spin up a server that recompiles the templates whenever they're changed.

Your editor will probably need Templ on your path, and you may also need to install it locally as a Go tool:

```sh
go install github.com/a-h/templ/cmd/templ@v0.3.1020
go get -tool github.com/a-h/templ/cmd/templ
```
